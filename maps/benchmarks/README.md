# Synthetic Benchmark Maps

These maps reproduce the categories and drone counts listed in `subject.md`,
but they are not the unpublished official evaluation maps. They provide stable
local regression cases for routing, capacity, priority, cycles, restricted
transit, and overlapping paths.
